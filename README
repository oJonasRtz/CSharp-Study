Some dotnet commands

	-dotnet new console -n ProjectName
		{Create a new project}

	-dotnet run
		{Run the project-with no params}

	-dotnet run -- args..
		{Run with args}

	-dotnet new classlib
		{Create a class library}

	-dotnet build
		{compile the project}
		{destiny: ~/Project/bin/Debug/netX.X/Project}
